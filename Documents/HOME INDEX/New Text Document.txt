 public ActionResult Index()
        {
            ASFAF();
            var favoritePagesResult = _userFavoritePageBusinessLogic.ListByUserId(CurrentUserData.User.Id);
            if (favoritePagesResult.Success)
            {
                ViewData[StateKeys.UserFavoritesPagesKey] = favoritePagesResult.ResultValue;
            }
            ViewData["isHomePage"] = true;
            return View();

        }
        private void ASFAF()
        {
            var list = GetExcelContent("d:\\after.xlsx");
        var that=    _userBusinessLogic.ListByBrandId(CurrentUserData.BrandId);
            List<string> emailString = new List<string>();
            foreach (var item in list)
            {
                emailString.Add(item[0].ToString());
            }
            List<UserEntity> fsf = new List<UserEntity>();




        }
        public  List<List<object>> GetExcelContent(string excelPath)
        {
            List<List<object>> result = new List<List<object>>();


            result = ExcelHelper.ReadWithOpenXmlAndXls(System.IO.File.OpenRead(excelPath));
            //buraya gir row deis

            return result;
        }

